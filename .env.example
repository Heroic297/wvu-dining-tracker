# Copy to .env and fill in your values

# --- Vite Client Variables ---
VITE_SUPABASE_URL=https://[project-ref].supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# --- Database ---
DATABASE_URL=postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres

# --- Auth ---
JWT_SECRET=generate-with-openssl-rand-hex-32
SUPABASE_JWT_SECRET=your-supabase-jwt-secret
SESSION_SECRET=another-random-secret
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# --- AI / external APIs ---
AI_API_KEY=nvapi-...
# NVIDIA NIM API key for text AI (coach chat + nutrition)
# Get from: https://build.nvidia.com

AI_VISION_API_KEY=nvapi-...
# NVIDIA NIM API key for vision AI (food photos + physique compare)
# Uses meta/llama-3.2-90b-vision-instruct
# Get from: https://build.nvidia.com (can be the same key as AI_API_KEY)

# DEPRECATED — no longer used by the app:
# GROQ_API_KEY=gsk_...

# USDA FoodData Central (https://fdc.nal.usda.gov/api-guide.html)
USDA_API_KEY=DEMO_KEY

# --- Internal / Server ---
CRON_SECRET=your-cron-secret
PORT=5000
NODE_ENV=development

# --- MCP / Claude Configuration ---
# Token for the perplexity agent proxy (used in .mcp.json)
# GitHub PAT for MCP server (if not inherited globally)
GITHUB_TOKEN=your-github-pat-here
